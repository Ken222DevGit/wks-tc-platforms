spring:
  application:
    name: tc-platform-sleuth
  cloud:
    config:
      discovery:
        enabled: true
        service-id: tc-platform-config
      profile: dev

info:
  author: ken
  description: 链路追踪
